# Stave Maps (`x_stave_em`) — Technical Documentation

> ServiceNow scoped application that plots **any ServiceNow table's records on an interactive map**. A map definition points at one or more source tables, resolves each record to latitude/longitude (from explicit coordinate fields or a location reference), and renders the results through either **Google Maps** (markers + heatmap + clustering) or **Esri ArcGIS** (web-map-based markers), with per-record tooltips and drill-through to the source record.

---

## 1. At-a-Glance

| Attribute            | Value                                                                 |
| -------------------- | -------------------------------------------------------------------- |
| **Name**             | Stave Maps                                                           |
| **Scope**            | `x_stave_em`                                                         |
| **Application sys_id** | `958e06c80fd29e00c783cd8ce1050e7f`                                 |
| **Scope ID**         | `958e06c80fd29e00c783cd8ce1050e7f`                                   |
| **Vendor**           | Stave Inc                                                           |
| **Version**          | 3.2.14                                                              |
| **JS Level**         | `es_latest`                                                         |
| **Created**          | 2016-03-16 by `yasser`                                              |
| **Last Updated**     | 2025-12-05 by `system`                                              |
| **Revisions**        | `sys_mod_count = 143`                                               |
| **Logo**             | 600×600 PNG, average color `#ff9933` (embedded in app manifest)     |
| **Source manifest**  | metadata/sys_app_958e06c80fd29e00c783cd8ce1050e7f.xml |
| **Build tooling**    | ServiceNow **Fluent** (`@servicenow/sdk` 4.6.0), package manager **Bun** |

**Short description (from manifest):**

> Display ServiceNow data on Google Maps and ArcGIS Map

> **Note on packaging.** This repository is a **Fluent** project: `src/fluent/index.now.ts` is the (currently empty) entry point, `now.config.json` declares the scope, and the deployed application definition lives as ServiceNow XML under `metadata/`. The `metadata/` folder is the source of truth for what runs on the instance; `src/fluent/generated/keys.ts` is auto-generated by `bun run types`.

---

## 2. Capabilities Overview

The application is organized around **one configuration model** (map + data layers) and **two interchangeable rendering engines**, plus supporting search and drill-through subsystems.

| Pillar                | Trigger model                    | Operates on                    | Purpose                                                                                     |
| --------------------- | -------------------------------- | ------------------------------ | ------------------------------------------------------------------------------------------ |
| **Map Definition**    | Configuration                    | `x_stave_em_maps` record       | Declares the map: provider, center, zoom, clustering, heatmap opacity/intensity, ArcGIS webmap ID. |
| **Data Layers**       | Configuration                    | `x_stave_em_map_data` records  | Per-map layers: which table to plot, how to resolve location, filter, tooltip script, icon, cluster/heatmap participation. |
| **Google renderer**   | On demand (View Map / portal)    | Google Maps JS API             | Markers with custom icons + **heatmap** (visualization lib) + **clustering** (MarkerClusterer), all toggleable per dataset. |
| **ArcGIS renderer**   | On demand (View Map / portal)    | Esri ArcGIS JS API 3.x         | Web-map-based markers with picture symbols, info popups, legend, and optional type-ahead search. Markers only (no heatmap/cluster). |
| **On-screen search**  | User action (ArcGIS only)        | All layer tables               | `IR_AND_OR_QUERY` full-text search across layer tables; centers/zooms the map on the picked result. |
| **Drill-through**     | Marker/tooltip click             | Source record                  | Opens the underlying ServiceNow record in an iframe modal ("View Record").                  |

---

## 3. Architecture

```
                   ┌──────────────────────────────────────────────────────────┐
                   │                        Stave Maps                         │
                   └──────────────────────────────────────────────────────────┘

  ── Configuration tables ──        ── Server engines (Script Includes) ──        ── Renderers ──

  x_stave_em_maps ───────┐
   (provider, center,    │
    zoom, cluster,       ├──► MapDefinitionContent.getMapContent(sysId) ──► JSON ──┬──► Google widget  (s-maps-widget)
    heatmap, webmap_id)  │        (saved-definition pipeline)                      │      Google Maps API + MarkerClusterer
                         │                                                         │      markers + heatmap + clustering
  x_stave_em_map_data ───┘    ServicePortalMapContent.getMapQuerybase(...) ──► JSON┤
   (table, location,              (ad-hoc / parameter-driven pipeline)            └──► ArcGIS widget  (s-map-widget-esri)
    filter, tooltip,                                                                     ArcGIS JS API 3.x (web map)
    icon, cluster/heatmap)   MapFunctions._combineJSON(...)  ← shared JSON assembler      markers + info popups + search

                              (legacy) getMapContentAjax  ──► XML ──► Show_Google_Map.do / Show_ArcGIS_Map.do
```

### 3.1 The "plot a table on a map" flow (saved definition)

1. An author creates an `x_stave_em_maps` record (provider, center, zoom, etc.) and one or more child `x_stave_em_map_data` layers (table, location field, filter, tooltip).
2. A user opens the map — via the **View Map** UI action on the map record, or by navigating to the portal page directly.
3. **View Map** (metadata/update/sys_ui_action_3d05c5820f125200c783cd8ce1050e1a.xml) saves the record and redirects by provider:
   - `map_provider = 0` (ArcGIS) → Service Portal page **`sp_map_esri?map_id=<sys_id>`**
   - `map_provider = 1` (Google) → Service Portal page **`sp_maps?map_id=<sys_id>`**
4. The portal widget's **server script** calls `x_stave_em.MapDefinitionContent.getMapContent(mapSysId, scope)`.
5. `getMapContent` loads the map, iterates each active `x_stave_em_map_data` layer, and for each layer:
   - queries the layer's `table` with its encoded `filter`;
   - resolves coordinates — either from explicit lat/long fields (`specific_location = true`) or by dereferencing the `location` reference field to a location record's `latitude`/`longitude`;
   - **merges records at identical coordinates** into a single weighted point (tooltips concatenated with `<hr>`, sys_ids appended, `weight` incremented);
   - evaluates the per-record `tooltip` script via `GlideScopedEvaluator`;
   - resolves the marker icon (attached image → DB image → default `stave_map_default_marker.png`).
6. `MapFunctions._combineJSON(...)` assembles `{errMsg, pointsOnMap, initialSetting, webmap, center, zoom, clusterOptions, heatmapOptions, geometry}` and returns it as JSON.
7. The widget's **client script** draws the map with the appropriate provider SDK and wires up marker clicks, the options panel, and the "View Record" modal.

---

## 4. Data Model — Table Reference

The app ships **2 tables**, both prefixed `x_stave_em_`. Both are plain `collection` tables (they do **not** extend `sys_metadata`).

### 4.1 `x_stave_em_maps` — "Map Definitions"

The map configuration record. `db_object_id = 999421fa0f229600c783cd8ce1050ed7`. Source: metadata/dictionary/x_stave_em_maps.xml

| Field               | Type            | Mand. | Default       | Hint / Notes                                                                                   |
| ------------------- | --------------- | :---: | ------------- | --------------------------------------------------------------------------------------------- |
| `name`              | string (100)    |   Y   |               | Map name.                                                                                     |
| `description`       | string (1000)   |       |               | Free-text description.                                                                        |
| `map_provider`      | choice (40)     |       | `0`           | `0` = ArcGIS, `1` = Google. Drives which renderer/portal is used and which fields show.        |
| `center_latitude`   | float           |       | `36.008522`   | Initial map center latitude. Validated to [−90, 90] on submit.                                 |
| `center_longitude`  | float           |       | `-95.221764`  | Initial map center longitude. Validated to [−180, 180] on submit.                              |
| `zoom_level`        | integer         |       | `4`           | Starting zoom (0 = lowest). Client scripts warn below 0 / above 22.                            |
| `webmap_id`         | string (32)     |   *   |               | ArcGIS.com web map ID. **Mandatory when `map_provider = 0`** (via UI policy). Ignored by Google. |
| `show_search`       | boolean         |       |               | Show the on-screen type-ahead search. Only visible for ArcGIS (via UI policy).                 |
| `enable_clustering` | boolean         |       |               | **Read-only** in the dictionary. Marker clustering toggle (Google).                            |
| `opacity`           | decimal (15)    |       | `0.6`         | Heatmap opacity, 0–1. Defaults to 0.6.                                                          |
| `max_intensity`     | integer         |       |               | Heatmap max intensity. Blank = dynamic auto-scaling by point concentration.                     |

> **Orphaned choice sets.** The app ships choice lists for a `type` field (`0` Circle / `1` Rectangle / `2` Polygon) and a "Hide Radius" UI policy references a `radius` field, but neither `type` nor `radius` exists in the current `x_stave_em_maps` dictionary — these are **legacy/vestigial** search-area-shape artifacts. See §8.2 / §6.3.

### 4.2 `x_stave_em_map_data` — "Map Data"

A data layer belonging to a map. `db_object_id = de2544700f48a200c783cd8ce1050e8a`. Source: metadata/dictionary/x_stave_em_map_data.xml

| Field                | Type                              | Mand. | Default | Hint / Notes                                                                              |
| -------------------- | --------------------------------- | :---: | ------- | ---------------------------------------------------------------------------------------- |
| `name`               | string (40)                       |   Y   |         | Layer name. `edge_encryption_enabled = true`.                                            |
| `map`                | reference → `x_stave_em_maps` (32) |       |         | Parent map. Indexed (`index2`).                                                          |
| `table`              | table_name (80)                   |   Y   |         | Source table whose records are plotted.                                                  |
| `filter`             | conditions (4000)                 |       |         | Condition-builder filter (dependent on `table`) scoping which records are plotted.        |
| `specific_location`  | boolean                           |       |         | If `true`, use explicit `latitudes`/`longitude` fields; if `false`, use the `location` reference. |
| `location`           | field_name (80)                   |   *   | `null`  | Field on `table` that holds the location reference. Mandatory unless `specific_location` (via UI policy). `allow_null=true`. |
| `latitudes`          | field_name (80)                   |       |         | Field on `table` holding latitude (note the **plural** column name). Shown only when `specific_location = true`. |
| `longitude`          | field_name (80)                   |       |         | Field on `table` holding longitude (**singular**). Shown only when `specific_location = true`. |
| `tooltip`            | script (8000)                     |       | *(see below)* | Server script producing the marker popup HTML. Vars: `content`, `gr`. **Field ACL-gated (script role).** |
| `show_icons`         | boolean                           |       | `true`  | Whether to draw marker icons for this layer.                                             |
| `icon`               | user_image                        |       |         | Uploaded marker icon.                                                                    |
| `icon_db`            | reference → `db_image` (32)       |       |         | DB image marker icon (alternative to `icon`). Indexed (`index`).                        |
| `icon_width`         | integer                           |       | `25`    | Icon width in px (defaults to 25).                                                       |
| `icon_height`        | integer                           |       | `25`    | Icon height in px (defaults to 25).                                                      |
| `include_in_cluster` | boolean                           |       |         | Participate in marker clustering (Google).                                               |
| `include_in_heatmap` | boolean                           |       |         | Participate in the heatmap layer (Google).                                               |

**Default `tooltip` script (as shipped):**

```javascript
/**
* You have access to these variables: "content" and "gr".
* content : The content of this variable will be shown in the pop-up menu if user clicks on its icon
* gr:  GlideRecord to the table with the filter applied.
*
* Usage: content = gr.short_description;
*
* Please note: The records without location information, and locations without latitude/longitude information
* are omitted from the glide record.
**/

content = gr.getDisplayValue();
```

**Indexes:** `icon_db`, `map`.

The app also ships ~30 `sys_dictionary_*` field overrides and `sys_documentation_*_en` label/help records for these two tables (tooltips, column widths, help text) — see metadata/update/.

---

## 5. Business Logic — Server-Side

### 5.1 Script Includes

All located in metadata/update/.

| Name                      | API name                          | Access          | Client-callable | Active | Responsibility                                                                                              |
| ------------------------- | --------------------------------- | --------------- | :-------------: | :----: | ---------------------------------------------------------------------------------------------------------- |
| `MapDefinitionContent`    | `x_stave_em.MapDefinitionContent` | public          |       No        | **Yes** | **Primary engine.** `getMapContent(sysId, mapScope)` builds the combined map JSON from a saved map + its data layers (query, coordinate resolution, duplicate-point merge, tooltip eval, icon resolution). Also `onScreenSearch()` for the ArcGIS type-ahead. |
| `ServicePortalMapContent` | `x_stave_em.ServicePortalMapContent` | package_private |     No        | **Yes** | **Ad-hoc engine.** `getMapQuerybase(...)` builds map JSON from parameters passed directly (table, encoded query, lat/lng or location field, icon, zoom, target page, webmap) rather than a saved record. Builds "View Record" links into tooltips. |
| `MapFunctions`            | `x_stave_em.MapFunctions`         | package_private |     No        | **Yes** | Shared JSON/utility helpers: `_prepareLocationData`, `_combineJSON`, `_prepareZoom`, `_prepareCenter`, `_isDuplicate`, `_prepareMapID`. No GlideRecord work. |
| `getMapContentAjax`       | `x_stave_em.getMapContentAjax`    | package_private |    **Yes**      | **No** | **Legacy / inactive.** Extends `global.AbstractAjaxProcessor`; returns the same data as an **XML** document. Location lookups hard-coded to `cmn_location`. Still referenced by the legacy `.do` UI pages. |

**Coordinate resolution.** Points come from either explicit latitude/longitude fields (`specific_location = true`) or a reference field resolved to a location record's `latitude`/`longitude`. `MapDefinitionContent` resolves the reference dynamically via `getRefRecord()`; the legacy `getMapContentAjax` and the ad-hoc `ServicePortalMapContent` hard-code the `cmn_location` table.

**Cross-scope call.** `onScreenSearch` uses the global `JSON` script include, granted via metadata/update/sys_scope_script_access_0b8427740f221600c783cd8ce1050ecf.xml.

> **Known rough edges (observed in source):** leftover debug logging (`gs.error(JSON.stringify(...))` inside the `MapDefinitionContent` loop), an `alert("here")` in the `map_checkbox` UI script, a `latitudes` vs `latitude` field-name inconsistency in the specific-location branch, and a likely-inverted `if (newValue === '')` guard in the "Check Zoom Level" onChange client script.

### 5.2 UI Action

| Name       | sys_id                            | Table            | Active | Where       | Condition                        | Behavior                                                             |
| ---------- | --------------------------------- | ---------------- | :----: | ----------- | -------------------------------- | ------------------------------------------------------------------- |
| `View Map` | `3d05c5820f125200c783cd8ce1050e1a` | `x_stave_em_maps` |   Y    | Form button | `gs.hasRole('x_stave_em.user')`  | Saves the record, then redirects to `sp_map_esri` (ArcGIS) or `sp_maps` (Google) by `map_provider`. A commented-out block previously pointed at the legacy `.do` pages. |

### 5.3 Client Scripts

All on `x_stave_em_maps`, active. Located in metadata/update/.

| Name                 | sys_id                            | Type     | Purpose                                                                                                        |
| -------------------- | --------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------ |
| `Check Lat/Lan Values` | `91d4558b0f446600c783cd8ce1050eeb` | onSubmit | Blocks submit unless `center_latitude` ∈ [−90, 90] and `center_longitude` ∈ [−180, 180].                     |
| `Check Zoom Values`  | `7970e94f0f446600c783cd8ce1050e4f` | onSubmit | Blocks submit if `zoom_level < 0`; warns (confirm) if `> 22` that high zoom may be ignored by the provider.    |
| `Check Zoom Level`   | `8b24b0cf0f046600c783cd8ce1050e86` | onChange | Advisory field messages for zoom `< 0`, `= 0`, `> 22`. *(Has a latent guard bug — `if (newValue === '')` — that suppresses the checks.)* |

### 5.4 UI Scripts (client-side JS libraries)

| Name                     | sys_id                            | Active | Scoped | Purpose                                                                                     |
| ------------------------ | --------------------------------- | :----: | :----: | ------------------------------------------------------------------------------------------ |
| `googleApiLoader`        | `2a4cf8a60f201b0055d1cd8ce1050e77` |   Y    |  No    | Injects the Google Maps JS API (`libraries=visualization,drawing,geometry`, `callback=initMap`); fires the `stave.map.initialized` CustomEvent. |
| `markerClusterer`        | `618eede50f542a00c783cd8ce1050e5f` |   Y    |  No    | Vendored Google Maps **MarkerClusterer** v1.0 (Apache-2.0). Per-zoom marker clustering.     |
| `map_checkbox`           | `8c08770e0fcd3a0055d1cd8ce1050ecb` |   Y    | **Yes** | Scoped `x_stave_em.map_checkbox`: `toggleCluster/toggleHeatmap/toggleMarkers(tableName)` → `redrawMap()`. |
| `StaveMapFunctions`      | `08c086ff0f54320055d1cd8ce1050eaf` |   Y    |  No    | `viewRecord(table, id)` — loads a record into `record_iframe` and shows the `#viewRecordModal`. |
| `service.sn-glideAjax`   | `df2aa98b0f092a40c783cd8ce1050ef7` |   Y    |  No    | Angular module `sn.glideAjax` with a `GlideAjax.getJSON(callback)` helper. Used by the ArcGIS search controller. |
| `Angular_1_3_framework`  | `d46aa98b0f092a40c783cd8ce1050ead` | **No** |  No    | Inactive. Loaded AngularJS 1.5.11 from the Google CDN.                                       |

---

## 6. UI Layer

Map rendering is implemented as **Service Portal AngularJS widgets** — not Jelly pages. Two portals, two widgets, one per provider.

### 6.1 Service Portal — Portals, Pages, Widgets

**Portals** (metadata/update/):

| Portal            | sys_id                            | URL suffix   | Homepage         | Purpose                     |
| ----------------- | --------------------------------- | ------------ | ---------------- | --------------------------- |
| Stave Maps        | `b3856ff40fc1b60055d1cd8ce1050e6c` | `sp_maps`    | `s_maps_widget`  | The **Google Maps** portal. |
| Stave Map Esri    | `eaddfdb10f413a0055d1cd8ce1050ec0` | `sp_map_esri`| `s_maps_widget_esri` | The **ArcGIS/Esri** portal. |

**Widgets** (metadata/update/):

| Widget          | sys_id                            | id                  | Renderer | Features                                                                              |
| --------------- | --------------------------------- | ------------------- | -------- | ------------------------------------------------------------------------------------ |
| S Maps Widget   | `10f96c7e0f4cb20055d1cd8ce1050eca` | `s-maps-widget`     | **Google** | `google.maps.Map` + Marker/InfoWindow, `visualization.HeatmapLayer`, `MarkerClusterer` (maxZoom 18). Per-dataset Markers/Heatmap/Cluster toggles, auto-centering, `spUtil.recordWatch` live refresh. |
| S_Map_Widget_Esri | `d8854d480ff4760055d1cd8ce1050e31` | `s-map-widget-esri` | **ArcGIS** | ArcGIS JS API 3.x (Dojo AMD) via `arcgisUtils.createMap(webMapID)`. Picture-marker symbols + info popups + legend. Falls back to a hard-coded Stave web map ID (`d5e02a0c1f2b4ec399823fdd3c2fdebd`). No heatmap/clustering. |

Both widget server scripts source data two ways: from a saved definition (`MapDefinitionContent.getMapContent`) or an ad-hoc query (`ServicePortalMapContent.getMapQuerybase`), returning a JSON `mapAttribute`. Both templates embed a "View Record" `<iframe>` modal.

**Pages** (metadata/update/):

| Page              | sys_id                            | id                 | Notes                                    |
| ----------------- | --------------------------------- | ------------------ | ---------------------------------------- |
| S Maps Widget google | `d4f96c7e0f4cb20055d1cd8ce1050eca` | `s_maps_widget`    | Google portal homepage.                  |
| S Maps Widget esri   | `853491000f38760055d1cd8ce1050ea1` | `s_maps_widget_esri` | Esri portal homepage.                  |
| googlemap admin   | `4e7910aa0f42b34055d1cd8ce1050e5d` | `googlemap_admin`  | Admin/config page.                       |
| Supermap          | `60243a7f1bb084d017a6b9dcdd4bcbbf` | `supermap`         | Combined map page.                       |

Layout is expressed in `sp_instance` / `sp_container` / `sp_row` / `sp_column` records (4 each). A CMS content-block iframe **Maps Record Iframe** (metadata/update/content_block_iframe_cc40ac8d0fddaa40c783cd8ce1050e0c.xml, frame `record_iframe`, 600×400) backs the legacy record viewer.

### 6.2 Legacy Jelly UI Pages

These predate the Service Portal widgets and call the **inactive** `getMapContentAjax` (XML). They are effectively deprecated in favor of the portal path.

| Page             | sys_id                            | Endpoint                       | Purpose                                             |
| ---------------- | --------------------------------- | ------------------------------ | -------------------------------------------------- |
| `Show_Google_Map` | `89572fe10f0ce200c783cd8ce1050e04` | `x_stave_em_Show_Google_Map.do` | Legacy Google map (marker/heatmap/cluster + options panel). |
| `Show_ArcGIS_Map` | `f90ce2440f169e00c783cd8ce1050e46` | `x_stave_em_Show_ArcGIS_Map.do` | Legacy ArcGIS map (ArcGIS JS API 3.45, web-map-based, Angular search). |
| `EM_About_stave`  | `45dab3c20f80274055d1cd8ce1050e75` | `x_stave_em_EM_About_stave.do`  | Redirect stub → external Stave URL.                |
| `EM_map_support`  | `a29abbc20f80274055d1cd8ce1050e52` | `x_stave_em_EM_map_support .do` | Redirect stub → `stavecorp.com/support/`.          |

### 6.3 UI Policies

Six active UI policies drive provider- and location-specific field visibility. Located in metadata/update/.

**On `x_stave_em_maps`:**

| Policy                          | sys_id                            | Condition                | Effect                                                            |
| ------------------------------- | --------------------------------- | ------------------------ | ---------------------------------------------------------------- |
| Show Webmap ID for ArcGIS only  | `344153ed0fc8e200c783cd8ce1050e88` | `map_provider = 0`       | `webmap_id` visible + **mandatory** for ArcGIS; hidden for Google. |
| Show Search for ArcGIS only     | `0af211f30f096a40c783cd8ce1050e6e` | `map_provider = 0`       | `show_search` visible for ArcGIS only.                            |
| Hide Radius                     | `16700fe00f5bf20055d1cd8ce1050ec2` | `type != 0` (not Circle) | Hides a `radius` field unless the shape is Circle. *(References the vestigial `type`/`radius` fields — see §8.2.)* |

**On `x_stave_em_map_data`:**

| Policy                          | sys_id                            | Condition                 | Effect                                                                       |
| ------------------------------- | --------------------------------- | ------------------------- | --------------------------------------------------------------------------- |
| Hide Lng lat                    | `9122ab6a0fdcf60055d1cd8ce1050eda` | `specific_location != true` | Hide `latitudes` + `longitude`; show + require `location`.                  |
| Remove mandatory on location    | `7b38a98c0f4f3a4055d1cd8ce1050e90` | `specific_location = true`  | `location` becomes optional + hidden (lat/long used instead).              |
| Hide checkbox                   | `788e96a50f65ba4055d1cd8ce1050efe` | `location IS NOT EMPTY`     | Hides the `specific_location` checkbox once a `location` field is set.      |

### 6.4 App Modules (Navigation)

Application menu **Stave Maps** (metadata/update/sys_app_application_998e86c80fd29e00c783cd8ce1050e5e.xml, device `browser`, role `x_stave_em.user`):

- **Core data** — Map Definitions (`x_stave_em_maps`), Map Data (`x_stave_em_map_data`).
- **Administration** (separator + admin lists) — UI Action Scripts, Script Includes (both scoped lists), UI Pages (*inactive*).
- **External links** (`_blank`) — Contact Stave Support (`staveapps.com/support`), Privacy Policy (`stavecorp.com/privacy-policy`), About Stave (`staveapps.com`). *Note: these three modules are gated by `x_stave_sc.user` (a different app's role) — likely a copy/paste artifact.*

---

## 7. Security Model

### 7.1 Roles (`sys_user_role`)

| Role                | sys_id                            | Description (verbatim)                                                          | Delegate | Grantable | Elevated | Assignable by |
| ------------------- | --------------------------------- | ------------------------------------------------------------------------------ | :------: | :-------: | :------: | ------------- |
| `x_stave_em.user`   | `118e86c80fd29e00c783cd8ce1050e5f` | *(empty)*                                                                       |   true   |   true    |  false   | (any)         |
| `x_stave_em.admin`  | `9f00763d977731105ea31fa00153afc5` | *(empty)*                                                                       |   true   |   true    |  false   | (any)         |
| `x_stave_em.script` | `e80b168393ddd6109d3b3ecdfaba108a` | "This role can only assigned by admins. Only users with the script role can edit scripts." | true | true | **true** | admin         |

No role contains another. `x_stave_em.script` is the only elevated-privilege role.

### 7.2 ACLs

The app ships **24 operation ACLs** (`sys_security_acl_*`) plus **24 ACL-to-role mappings** (`sys_security_acl_role_*`).

- **Both tables — full CRUD gated by the single `x_stave_em.user` role.** `read` / `create` / `write` / `delete` on `x_stave_em_maps` and `x_stave_em_map_data` all require `x_stave_em.user` (`admin_overrides = true`). There is no separate read-only vs. read-write tier at table level.
- **`query_range` ACLs are `public`** (authenticated, subject to the derived read attribute) and auto-generated (the only ACLs where admin does not override).
- **Field-level tightening on `x_stave_em_map_data.tooltip`** (a script field): `create` / `list_edit` require `x_stave_em.admin`; `write` requires the elevated `x_stave_em.script` role (the old admin-gated `write` ACL, `57c0fafd…`, is **inactive**). This matches the `script` role's stated purpose.
- **UI page ACLs** (`read`) require `x_stave_em.user` (the map-support page also has an `snc_internal` variant). The `getMapContentAjax` client-callable include allows `x_stave_em.user` ("Allow access to anybody").

### 7.3 Other security artifacts

- **Crypto caller policy** (metadata/update/sys_kmf_crypto_caller_policy_80ca25b397f571105ea31fa00153aff8.xml) — auto-generated `AutoGen-global.com_snc_platform_security_oauth_glideencrypter`, `result = track` (audit only, not blocking). Tracks the app's use of the OAuth GlideEncrypter; supports the `edge_encryption_enabled` fields (`map_data.name`).
- **Embedded help role mappings** — `x_stave_em.admin` (order 10) and `x_stave_em.script` (order 1000) see in-app embedded help.

---

## 8. Configuration

### 8.1 System Properties

| Property                            | Type    | Value                              | Purpose                                                                        |
| ----------------------------------- | ------- | ---------------------------------- | ----------------------------------------------------------------------------- |
| `x_stave_em.searchResult.zoomLevel` | integer | `6`                                | Zoom level applied when the user clicks a search result to center the map.      |
| `x_stave_em.google_map_scope`       | string  | `v10f96c7e0f4cb20055d1cd8ce1050eca` | Reference to the Google map widget scope.                                       |
| `x_stave_em.esri_map_scope`         | string  | `vd8854d480ff4760055d1cd8ce1050e31` | Reference to the Esri map widget scope.                                         |

The renderers also read platform properties `google.maps.key` (Google Maps API key) and `glide.servlet.uri` (icon base path).

### 8.2 Choice Lists

| Field / choice set                     | Values                                              | Status  |
| -------------------------------------- | --------------------------------------------------- | ------- |
| `x_stave_em_maps.map_provider`         | `0` ArcGIS, `1` Google                              | Active  |
| `x_stave_em_maps.type`                 | `0` Circle, `1` Rectangle, `2` Polygon              | **Orphaned** — no matching dictionary field; a search-area-shape remnant referenced only by the "Hide Radius" UI policy. |

### 8.3 Cross-Scope Table Access

**18 read-only** `sys_scope_table_access` grants let the app plot records from other scopes/tables on maps (create/write/delete all false):

- **Global:** `task`, `incident`, `cmdb_ci`, `cmdb_ci_database`, `cmdb_ci_computer`, `cmdb_ci_server`, `cmdb_ci_datacenter`, `alm_asset`, `fm_cubicle`, `cmn_location`, `sys_user`, `sys_user_has_role`, `sys_db_object`, `wm_order`
- **Stave ProcurementPath:** `x_stave_cm_key_date`, `x_stave_cm_contract`
- **Social Knowledge:** `kb_social_qa_question`
- **Stave FM:** `x_stave_fm_asset_tracker`

### 8.4 Table Licensing

Both tables ship `ua_table_licensing_config` records with `license_model = none` (no paid enforcement), all ops allowed, `owner_condition = sys_created_by = current user`.

### 8.5 Other

- **Database view** `x_stave_em_test` (metadata/update/sys_db_view_732fdcf5939d52109d3b3ecdfaba1019.xml) — empty/leftover test view (Oct 2024).
- ~11 `db_image_*` records — marker/cluster/logo image binaries used as map icons.

---

## 9. Usage / How-To

### 9.1 Plot a table using a location reference (e.g. Assets by location)

1. Create an `x_stave_em_maps` record: set `name`, `map_provider` (Google or ArcGIS), `center_latitude`/`center_longitude`, `zoom_level`. For ArcGIS, also set `webmap_id` (mandatory).
2. Add an `x_stave_em_map_data` layer:
   - `table = alm_asset` (or any granted table);
   - leave `specific_location = false`;
   - `location = <reference field that points at cmn_location>` (mandatory in this mode);
   - `filter = <optional condition>`;
   - `tooltip = content = gr.getDisplayValue();` (or a richer script).
3. Save, then click **View Map**. You're redirected to `sp_maps` (Google) or `sp_map_esri` (ArcGIS) and the records render as markers.

### 9.2 Plot a table using explicit latitude/longitude columns

1. On the `x_stave_em_map_data` layer, set `specific_location = true`.
2. The `location` field disappears; set `latitudes` and `longitude` to the two coordinate field names on the source table.
3. Configure the icon (`icon` upload or `icon_db`), `icon_width`/`icon_height`, and tooltip as needed.

### 9.3 Enable heatmap / clustering (Google only)

1. On the map, set `opacity` (0–1) and optionally `max_intensity` (blank = auto-scale).
2. On each data layer, tick `include_in_heatmap` and/or `include_in_cluster`.
3. In the rendered Google widget, use the **Map Options** panel to toggle Markers / Heatmap / Cluster per dataset at runtime.

### 9.4 Use an existing ArcGIS.com web map

1. Set `map_provider = 0` (ArcGIS) on the map record.
2. Set `webmap_id` to the ID from the ArcGIS.com item URL — e.g. for `…item.html?id=d5e02a0c1f2b4ec399823fdd3c2fdebd`, the ID is `d5e02a0c1f2b4ec399823fdd3c2fdebd`.
3. Optionally tick `show_search` to enable the on-screen type-ahead search (ArcGIS only).

### 9.5 Add a new source table

Because the app enforces cross-scope table access, plotting a table outside those in §8.3 requires adding a new `sys_scope_table_access` grant (read) for that table before it can be queried by the map engines.

---

## 10. Extension Points

- **Ad-hoc maps without a saved definition.** `x_stave_em.ServicePortalMapContent.getMapQuerybase(...)` builds a map from parameters alone — embed the `s-maps-widget` / `s-map-widget-esri` widget on any portal page and pass `map_table`, `map_query`, coordinate fields, icon, and `target_page` to render a map of an arbitrary query.
- **Custom marker icons.** Per layer via uploaded `icon`, a `db_image` reference (`icon_db`), and `icon_width`/`icon_height`; falls back to `stave_map_default_marker.png`.
- **Rich tooltips.** The `tooltip` script field runs server-side with `content` and `gr` in scope — build HTML popups, links, or computed values. (Editing it requires the `x_stave_em.script` role.)
- **New source tables.** Grant read access via `sys_scope_table_access` (§8.3, §9.5).
- **Provider swap.** A map can be flipped between Google and ArcGIS by changing `map_provider`; the View Map action routes to the matching portal automatically.

---

## 11. Appendix — Key Identifiers & File Map

| Artifact                                  | sys_id / id                        | Source file                                                                 |
| ----------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------- |
| Application                               | `958e06c80fd29e00c783cd8ce1050e7f` | metadata/sys_app_958e06c80fd29e00c783cd8ce1050e7f.xml |
| Application menu                          | `998e86c80fd29e00c783cd8ce1050e5e` | metadata/update/sys_app_application_998e86c80fd29e00c783cd8ce1050e5e.xml |
| Table `x_stave_em_maps` (db_object_id)    | `999421fa0f229600c783cd8ce1050ed7` | metadata/dictionary/x_stave_em_maps.xml   |
| Table `x_stave_em_map_data` (db_object_id) | `de2544700f48a200c783cd8ce1050e8a` | metadata/dictionary/x_stave_em_map_data.xml |
| Script Include `MapDefinitionContent`     | `377402bd0f6cb20055d1cd8ce1050e62` | metadata/update/sys_script_include_377402bd0f6cb20055d1cd8ce1050e62.xml |
| Script Include `ServicePortalMapContent`  | `91d54efd0f6cb20055d1cd8ce1050e3a` | metadata/update/sys_script_include_91d54efd0f6cb20055d1cd8ce1050e3a.xml |
| Script Include `MapFunctions`             | `d6b94afd0f6cb20055d1cd8ce1050e8c` | metadata/update/sys_script_include_d6b94afd0f6cb20055d1cd8ce1050e8c.xml |
| Script Include `getMapContentAjax` (legacy) | `45a2404d0f221600c783cd8ce1050ee8` | metadata/update/sys_script_include_45a2404d0f221600c783cd8ce1050ee8.xml |
| UI Action `View Map`                      | `3d05c5820f125200c783cd8ce1050e1a` | metadata/update/sys_ui_action_3d05c5820f125200c783cd8ce1050e1a.xml |
| Widget `S Maps Widget` (Google)           | `10f96c7e0f4cb20055d1cd8ce1050eca` | metadata/update/sp_widget_10f96c7e0f4cb20055d1cd8ce1050eca.xml |
| Widget `S_Map_Widget_Esri` (ArcGIS)       | `d8854d480ff4760055d1cd8ce1050e31` | metadata/update/sp_widget_d8854d480ff4760055d1cd8ce1050e31.xml |
| Portal `Stave Maps` (Google)              | `b3856ff40fc1b60055d1cd8ce1050e6c` | metadata/update/sp_portal_b3856ff40fc1b60055d1cd8ce1050e6c.xml |
| Portal `Stave Map Esri` (ArcGIS)          | `eaddfdb10f413a0055d1cd8ce1050ec0` | metadata/update/sp_portal_eaddfdb10f413a0055d1cd8ce1050ec0.xml |
| Role `x_stave_em.user`                    | `118e86c80fd29e00c783cd8ce1050e5f` | metadata/update/sys_user_role_118e86c80fd29e00c783cd8ce1050e5f.xml |
| Role `x_stave_em.admin`                   | `9f00763d977731105ea31fa00153afc5` | metadata/update/sys_user_role_9f00763d977731105ea31fa00153afc5.xml |
| Role `x_stave_em.script`                  | `e80b168393ddd6109d3b3ecdfaba108a` | metadata/update/sys_user_role_e80b168393ddd6109d3b3ecdfaba108a.xml |
| Property `searchResult.zoomLevel`         | `15733e570fcd2a40c783cd8ce1050e23` | metadata/update/sys_properties_15733e570fcd2a40c783cd8ce1050e23.xml |

---

## 12. Version Notes

- **3.2.14** — Current version. App manifest `sys_mod_count = 143` cumulative revisions.
- **Created** — 2016-03-16 by `yasser`; logo uploaded 2017 by `jodi.shaw`. Long-lived app (originally CMS/Jelly-based, later migrated to Service Portal widgets).
- **Two parallel rendering pipelines exist.** The modern path (View Map → `sp_maps`/`sp_map_esri` portal widgets → active JSON script includes) supersedes the legacy `.do` Jelly pages (which still call the **inactive** `getMapContentAjax` returning XML). The `.do` pages are effectively deprecated.
- **Vestigial artifacts** — an orphaned `type` choice set (Circle/Rectangle/Polygon) and a "Hide Radius" UI policy reference `type`/`radius` fields not present in the current dictionary; a Mapbox CSS include is wired but unused; an empty `x_stave_em_test` DB view; three nav links gated by another app's `x_stave_sc.user` role.
- **Build convention** — Delivered as a **Fluent** project. `metadata/` holds the ServiceNow XML source of truth; `dist/` and `src/fluent/generated/keys.ts` are auto-generated (`bun run build` / `bun run types`).

---

*Document generated from source XML under `metadata/` against scope `x_stave_em` v3.2.14.*
